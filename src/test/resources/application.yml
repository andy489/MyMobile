spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    defer-datasource-initialization: true
  sql:
    init:
      mode: never
  flyway:
    enabled: false

  datasource:
    url: "jdbc:hsqldb:mem:testdb;sql.syntax_mys=true"
#    postgresSQL (psql)
#    url:"jdbc:hsqldb:mem:testdb;sql.syntax_pgs=true"
#    oracle
#    url:"jdbc:hsqldb:mem:testdb;sql.syntax_ora=true"

mobilele:
  remember-me-key: test

mail:
  app-mail: "mobilele@mobilele.com"
  host: "localhost"
  port: 3333
  username: test
  password: test

open.exchange.rates:
  schema: https
  host: dummy
  path: /api/latest.json
  app_id: test
  symbols: BGN,EUR,GBP
  decimal-precision: 3
  enabled: false

